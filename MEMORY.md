# AI SCRM Memory

## Project Snapshot

- Project name: AI SCRM
- Workspace root: `/Users/chaoyun/Desktop/AI SCRM`
- Initial memory date: 2026-06-24
- Apparent stack: Markdown product documents, static HTML prototype, JSON page inventories, screenshot assets.

## Known Files And Assets

- `SCRM产品方案.md`: Product plan document.
- `好客私域通业务模块分析.md`: Detailed business-module analysis based on screenshots, page trees, page inventory, and manual subpage captures.
- `线下门店私域MVP点击原型.html`: Static clickable prototype.
- `线下门店私域实现方案.md`: Implementation plan document.
- `scrm_page_inventory.json`: Page inventory data.
- `scrm_manual_subpages_v2.json` and `scrm_manual_subpages_remaining.json`: Manual subpage data.
- `screenshots/`, `page_states/`, `采集截图与页面树/`: Screenshot and page-state assets.

## Decisions

- 2026-06-24: Created project-local `AGENTS.md` and `MEMORY.md` because the workspace lacked both files and `~/.codex/templates/` did not exist.
- 2026-06-24: Local static preview runs from the project root on `http://127.0.0.1:8765/`; prototype entry is `/线下门店私域MVP点击原型.html`.
- 2026-06-24: `好客私域通业务模块分析.md` treats the product as a full Enterprise WeChat SCRM covering acquisition attribution, customer operation, sales conversion, service, risk control, data analytics, and enterprise administration. Pages with likely capture/navigation anomalies are explicitly marked as needing recheck.
- 2026-06-24: Rebuilt `线下门店私域MVP点击原型.html` around the current offline-store private-domain scope: dashboard metrics, store private-domain config, store-staff codes, material-code assignment, attribution lock, identity merge, customer handover queue, guide workbench/sidebar, official WeCom touchpoints, group operations, and tag/rule settings.
- 2026-06-24: Audit finding for the prototype: first-level modules cover the current-period closed loop, but many second-level drawers are still explanatory stubs. Completion should be judged by whether each action changes visible state or returns to the next step in the offline-store private-domain workflow, not merely by having an entry button.
- 2026-06-24: Reworked the `标签与规则` page from a mixed parameter table into a customer tag management workspace modeled on Haoke SCRM logic: tag groups, tag list, WeCom tag sync, usage entry points, automatic tagging rules, lifecycle/pre-tag rules, and system parameters separated into tabs.
- 2026-06-24: Added a lightweight in-page state layer to the HTML prototype for priority second-level closed loops: adding store reception staff creates a visible staff/code row, saving store private-domain config refreshes the config card, submitting handover updates customer ownership and queue status, new welcome/SOP rules append to touch-operation lists, and customer detail drawers now switch by selected row.
- 2026-06-24: Added a consistent page guide card to every first-level page in the prototype. Each guide explains the page task, the first recommended operation, and where to go next, so users can understand the offline-store private-domain workflow while still using dense operational screens.
- 2026-06-24: Upgraded the prototype page hierarchy from simple guide cards to a flow-based object/action/result layout. Store config, customer acquisition codes, customer pool, guide workbench, touch operations, and customer group operations now show their position in the offline-store private-domain closed loop, the page objects, recommended action, and expected result before the dense tables/actions.
- 2026-06-24: Removed the global workflow guide cards and the dashboard goal notice from the prototype because they added visual noise after the page-level hierarchy was clear enough. Reworked the tag/rule drawers into operational flows: create tag plus optional auto-tag rule on one page, auto-tag impact preview with visible calculation, selectable store scope, tag-group creation with label association/order controls, and WeCom tag sync with system-tag mapping.
- 2026-06-24: Rebuilt the `标签与规则` page as a beginner-friendly workbench instead of a tag inventory page. The page now leads with a three-step operation order, shows the data flow from sources to tag processing to business usage, separates tag dictionary, auto-tag rules, usage relationships, and system parameters into clear tabs, and replaces vague jump cards with explicit usage scenarios explaining customer-pool filtering, mass-send targeting, code-based auto-tagging, and guide workbench usage.
- 2026-06-24: Created separate worktree `/Users/chaoyun/Desktop/AI SCRM-tag-rules-redesign` on branch `codex/tag-rules-screenshot-redesign` for a full `标签与规则` redesign without overwriting the current workspace. In this worktree, the page is rebuilt to match the provided Haoke-style screenshot: top tabs `客户标签 / 标签统计 / 管理规则`, toolbar controls for adding tag groups, help, department scope, and WeCom sync, plus row-based tag groups with visible scope, tag chips, add, and edit actions.
- 2026-06-24: Corrected the tag-group model in the screenshot-style redesign: `标签统计` must display both tag and tag-group fields, and the edit drawer for a tag group should edit group-level fields (name, visible scope, departments, owner, display order, group tags, and business usage), not a generic tag-management table.
- 2026-06-24: Closed the pre-tag rule save loop in the screenshot-style tag redesign. `管理规则` now separates automatic tagging rules from pre-tag rules; saving a pre-tag rule returns to the management tab, shows a top success notice, and writes the saved activity/tag/period into the visible `预打标签规则` table.
- 2026-06-24: Updated the store/code workflow per product review: `客户入池管理` is renamed to `客户管理` and now shows customer search/list data instead of code lists; store detail `门店活码生命周期` has search and pagination controls, removes redundant closed-loop copy and “查看全部活码”, adds `查看客户` actions that jump to filtered customer data, and handover now requires syncing Enterprise WeChat leave/transfer data before showing the handover list.
- 2026-06-24: Customer detail drawer was widened and changed from a read-only profile into an operable customer-guide relationship panel. It now uses neutral section titles (`客户详情`, `来源归属`, `服务归属`, `关联导购信息`), tracks guide relationship start/end dates, supports adding/ending guide links, setting/transferring the main follow-up guide, and adding follow-up records with visible timeline feedback.
- 2026-06-24: Simplified the store module wording to plain `门店` and moved `处理导购交接` next to `添加接待导购`. Customer management now leads with batch actions, changes row operations to `客户详情 / 触达客户`, and single-customer touch creates a task plus timeline entry.
- 2026-06-24: Customer detail timeline entries should be auditable: every customer-guide relationship, follow-up, touch, or handover action records an exact timestamp, related customer/guide/operator, and a short result description.
- 2026-06-24: Customer group operations now follow the Haoke-style secondary menu structure under `客户群运营`: `客户群管理 / 入群欢迎语 / 客户群群发 / 群SOP / 群日历 / 客户群提醒 / 客户群标签`. Each child page must expose its own object list and save loop instead of being a static explanation page.
- 2026-06-24: Closed the branch-analysis gaps in the tag-rules redesign worktree by adding final override renderers for customer management and customer group operations. Customer management now has batch actions plus row-level customer detail/touch actions, and customer group secondary pages keep visible state after creating groups, mass sends, welcome/SOP/calendar/reminder rules, and group tag groups.
- 2026-06-25: Based on `好客私域通客户转化逻辑分析.md`, the MVP branch now covers the missing conversion loop after private-domain acquisition: behavior radar creates high-intent signals and guide tasks, customer management shows intent score and sales stage, customer detail can create leads/opportunities and record orders/payments, and conversion results flow back into customer tags, lifecycle, timelines, sales pipeline, and customer-group contribution.
- 2026-06-25: The tag-rules redesign worktree is published to GitHub at `https://github.com/6Wendy6/jianghu-scrm` on branch `codex/tag-rules-screenshot-redesign`.
- 2026-06-25: The Go backend now uses an in-memory business API layer as the first implementation target. It covers the prototype closed loops for store private-domain config, guide QR lifecycle, handover sync/submit, customer relations/touch/conversion, customer group operations, and tag/rule governance before introducing a database.
- 2026-06-26: Performance direction is to evolve the demo toward an industrial SCRM baseline for roughly 2,000-3,000 concurrent online users before business integration. The first implemented backend guardrails are stateless API runtime defaults: HTTP timeouts, graceful shutdown, request IDs, access logs, CORS support for request IDs, and per-client-IP token-bucket rate limiting. Next performance steps should prioritize database-backed pagination, event inbox/queue for Enterprise WeChat callbacks, async workers for batch tasks, Redis caching/limits, and metric snapshots for dashboards.
- 2026-06-26: Added the second performance baseline step while keeping the prototype backward compatible. List endpoints now support optional paginated envelopes via `page/pageSize` or `limit/offset` while returning legacy arrays when no pagination parameters are present. Added `/api/events/inbox` as an idempotent in-memory event write path for future WeCom callbacks and scan events. Added `backend/migrations/001_industrial_baseline.sql` as the PostgreSQL target schema covering customers, identities, attribution, guide relations, store codes, tags, customer events, event inbox, async tasks, and metric snapshots.
- 2026-06-26: Added the third performance baseline step: an in-memory worker loop that advances event inbox records from queued to processed and processes async task items in batches. Added task tracking APIs (`/api/tasks`, `/api/tasks/{id}`, `/api/tasks/{id}/items`) and `POST /api/customers/batch-tags-async` as the first async batch operation. The synchronous `batch-tags` endpoint remains for prototype compatibility, while production clients should use async tasks for high-volume operations.
- 2026-06-26: Started the database migration path without breaking the demo. The backend now has optional PostgreSQL mode via `SCRM_DATABASE_URL`, tuned connection pool settings, startup ping, optional `SCRM_AUTO_MIGRATE`, and `/api/health` reports storage mode plus DB pool stats. Default remains memory mode. Added idempotent demo seed migration `backend/migrations/002_seed_demo.sql`. Next step is to migrate repositories endpoint by endpoint, starting with customers and event inbox/tasks.
- 2026-06-26: Migrated the first customer repository paths to PostgreSQL mode. When `SCRM_DATABASE_URL` is set, `GET /api/customers` uses SQL filtering/counting plus `LIMIT/OFFSET`, `POST /api/customers` upserts customer profiles into `customers`, and `GET /api/customers/{id}` loads the customer profile with tag bindings and guide relations. Memory mode remains unchanged. Customer mutations beyond create/read and async task/event persistence are still memory-backed pending later repository migration.
- 2026-06-26: Migrated event inbox and async task paths to PostgreSQL mode. `POST/GET /api/events/inbox`, `GET/POST /api/tasks`, task detail/items, and `POST /api/customers/batch-tags-async` now use database persistence when `SCRM_DATABASE_URL` is set. The worker processes DB-backed queued events and task items, uses `FOR UPDATE SKIP LOCKED` for event claims, persists async customer tag bindings to `customer_tags`, updates `customers.updated_at`, and writes `customer_events` audit records. Memory mode remains unchanged.
- 2026-06-26: Added local PostgreSQL infrastructure and verified DB mode end to end. `docker-compose.yml` starts a tuned PostgreSQL 16 container on host port `15432`, `.env.example` documents DB/runtime settings, root package scripts now include `db:up`, `db:down`, `db:logs`, and `dev:backend:db`. Ran migrations and seed data successfully against the Docker DB, verified DB-backed health, customer pagination, event inbox processing, async batch tagging, and customer tag reads. The currently running backend on `127.0.0.1:8080` is in PostgreSQL mode with `SCRM_AUTO_MIGRATE=true`.
- 2026-06-26: Migrated tag governance paths to PostgreSQL mode. `GET/POST /api/tags`, tag toggle/rename, `GET /api/tags/{name}/customers`, and `GET/POST /api/tag-groups` now use `tag_groups`, `tags`, and `customer_tags` when `SCRM_DATABASE_URL` is set. Tag list queries aggregate customer counts in SQL, tagged-customer drilldowns use paginated DB reads, and async batch-tag output is now visible in the tag governance API. Memory mode remains unchanged.
- 2026-06-26: Added DB-backed dashboard metric snapshots. In PostgreSQL mode, `GET /api/summary` reads global metrics from `metric_snapshots` and refreshes missing snapshots on demand; `POST /api/metrics/refresh` explicitly recomputes today or a requested date. Snapshot metrics cover today pool, attribution rate, pending items, touch coverage, seven-day trend, and generated operation suggestions. Production should protect refresh with admin permissions and move refresh cadence into a scheduled worker.
- 2026-06-26: Added a short-TTL in-process response cache for PostgreSQL-mode hot reads. `GET /api/summary`, customer list/detail, tag list, tagged-customer drilldowns, and tag-group list use URI-scoped cache keys and return `X-SCRM-Cache` hit/miss headers. Relevant writes invalidate prefixes for summary, customers, tags, tag groups, and tagged-customer lookups. This is a single-instance optimization; multi-instance production should move the same key/prefix strategy to Redis or another distributed cache.
- 2026-06-26: Added optional Redis-backed distributed response cache while preserving local cache fallback. When `SCRM_REDIS_ADDR` is configured, the backend uses Redis for the same hot-read cache keys and prefix invalidation; otherwise it keeps the in-process cache. Redis local infrastructure runs via Docker Compose on host port `16379`, and `npm run dev:backend:db:redis` starts the API with PostgreSQL plus Redis cache. Go remains pinned to `1.22`; go-redis is pinned to a Go 1.22-compatible version.
- 2026-06-26: Added observability and load-test baseline tooling. `GET /api/metrics` now emits Prometheus-compatible request counters, duration histograms, runtime memory/goroutine gauges, PostgreSQL pool gauges, cache backend metrics, and event/task backlog gauges with normalized route labels. `scripts/load-test.mjs` plus `npm run perf:smoke` / `npm run perf:load` provide local hot-read pressure tests that report QPS, failure rate, latency percentiles, status distribution, and cache hit rate.
- 2026-06-26: Migrated store and guide repository paths to PostgreSQL mode. Added `backend/migrations/003_store_guide_baseline.sql` with `stores`, `guides`, and `guide_events`, seeded the demo stores/guides, and backed `GET /api/stores`, store config updates, store guide list/create, `GET /api/guides`, guide pause/remove, lifecycle reads, and QR download from DB when `SCRM_DATABASE_URL` is set. Guide lifecycle actions now write audit rows to `guide_events`; memory mode remains unchanged.
- 2026-06-26: Migrated customer operation actions to PostgreSQL mode. Customer detail now loads persistent tag bindings, guide relations, deal amount, and recent `customer_events`; touch tasks, follow-ups, customer-guide relation mutations, lead creation, order recording, lost marking, and synchronous batch tagging now update DB state and append audit events in transactions. Large batch tagging should still prefer the async task endpoint for production traffic.
- 2026-06-26: Migrated operation configuration modules to PostgreSQL mode. Added `backend/migrations/004_operation_config_baseline.sql` for touch rules, customer groups, group mass/welcome/SOP/calendar/reminder/tag objects, auto tag rules, and pre-tag rules. The corresponding `GET/POST /api/touches`, `/api/groups*`, and `/api/tag-rules/*` endpoints now persist to DB in PostgreSQL mode while preserving the prototype response shapes.
- 2026-06-26: Migrated store handover workflow to PostgreSQL mode. Added `backend/migrations/005_store_handover_baseline.sql` for per-store sync state and handover items; `POST /api/stores/{id}/handover/sync` and `/submit` now persist handover state, update customer owner/guide relations, write customer audit events, and invalidate dashboard snapshots. Demo seed migration `002_seed_demo.sql` now uses conflict `DO NOTHING` so startup migrations do not overwrite operational customer, relation, code, tag, or attribution state.
- 2026-06-26: Refactored the backend from a single large `main.go` into responsibility-based files within the same Go package, with no intended behavior change. `main.go` now only wires process startup/shutdown; source modules now separate config/DB, middleware/cache/metrics, models, bootstrap/routing, dashboard, stores, guides, handover, customers, customer actions, batch tags, tags, groups/rules, events/tasks, workers, and shared helpers.
- 2026-06-26: Added key engineering hardening for production-style operation. Runtime config now supports optional `SCRM_API_TOKEN` protection, `SCRM_BUSINESS_TIME_ZONE` defaults to `Asia/Shanghai` and is applied to Go timestamps plus PostgreSQL sessions, and DB-backed workers now use configurable retry/dead-letter behavior via `SCRM_WORKER_MAX_ATTEMPTS` and `SCRM_WORKER_RETRY_DELAY`. Migration `006_worker_retry_deadletter.sql` adds retry scheduling fields and task `last_error`; `/api/metrics` exposes `scrm_task_items_dead_letter`.
- 2026-06-26: Added a root `README.md` for business-test handoff. It documents clone/start commands, safe business-data testing advice, the engineering optimizations already completed, module responsibilities, key environment variables, load-test entry points, and current production-readiness boundaries. The handoff branch should be `codex/business-test-baseline`.
- 2026-07-04: Implemented the Enterprise WeChat store manager code wrapping direction: bind existing contact-way `config_id` values instead of creating new QR codes, maintain a per-store guide pool in SCRM tables, activate/switch with `update_contact_way`, and process `add_external_contact` / `add_half_external_contact` callbacks into assignment records before rotating to the next guide. Local smoke tests should set `WECOM_CONTACT_WAY_DRY_RUN=true` to verify A/B/C rotation without changing live Enterprise WeChat contact-way configuration.
- 2026-07-04: Frontend product boundary for Enterprise WeChat codes is fixed as three separate pages: `企业微信活码资产` shows only the original WeCom contact-way asset/config_id layer, `门店店长码` shows SCRM business wrapping plus guide rotation for an existing config_id, and `客户分配记录` is the audit table for callback assignment results. Product copy should explicitly state that no new QR code is generated; SCRM only binds an existing config_id and later updates the same config_id behind the scenes with `update_contact_way`.
- 2026-07-04: The store manager code module is now positioned as a headquarters-controlled private-domain acquisition distribution system, not a single-store QR tool. MVP navigation should include `总部工作台`, `企业微信活码资产`, `门店店长码`, `客户分配记录`, and `异常监控`. Headquarters sees all stores, assets, bindings, rules, rankings, and anomalies; store managers operate their own guide pool and daily allocation; guides only own their assigned customers in the later RBAC implementation.
- 2026-07-04: Started `SCRM-MVP-02` real Enterprise WeChat chain integration. Added `/api/scrm/wecom/doctor` for configuration/access-token/get_contact_way/update_contact_way dry-run checks, binding-time config_id validation before creating store manager code wrappers, retry task persistence for update_contact_way failures, manual retry endpoints, integration-log output, lightweight header-based SCRM scope filtering for store manager/guide reads, and frontend doctor/retry visibility in `总部工作台` plus `异常监控`. Current local run keeps `WECOM_CONTACT_WAY_DRY_RUN=true`; doctor may show access_token attention until live Enterprise WeChat credentials/network permissions are corrected.
- 2026-07-06: Added the visible `资料补全中心` MVP to the active iframe prototype (`frontend/public/prototype.html`). It is placed after `客户分配记录` and before `异常监控`, scans four object types (门店、店长、导购、客户), applies required/important/optional completeness rules, computes completeness scores and P0/P1/P2 priorities, supports filters, right-side single-object completion, batch completion, issue ignore/unable status, and completion logs. P0 data-quality issues now also feed into the headquarters `异常监控`. The page intentionally bypasses the demo-data sanitizer because it is an operational prototype with local scan state; a later backend step should persist the same concepts as `data_quality_rules`, `data_quality_issues`, `data_quality_snapshots`, and `data_quality_change_logs`.
- 2026-07-06: Added `客户运营中心` to the active iframe prototype as the next stage from customer intake to customer operation. It sits after `资料补全中心`, includes customer lifecycle stages, profile tags, follow-up records, SOP tasks, material/script recommendations, headquarters/store/guide view toggles, stage filtering, task handling, headquarters task dispatch, stage transition logging, follow-up timeline writes, and material creation/copy flows. Overdue SOP tasks plus first-follow/dormant customer risks now feed into `异常监控`. This is still local prototype state; future industrialization should move lifecycle, tags, followups, tasks, materials, and operation logs into backend APIs with RBAC data filtering.
- 2026-07-06: Industrialized the `客户运营中心` data loop on the in-memory backend. Added customer-operation models and APIs for bootstrap/list/detail, lifecycle stage changes, customer tags, follow-up records, customer timelines, SOP task create/process/complete/ignore, material creation/update/delete, and operation exceptions. Header-based scope filters (`X-SCRM-Role`, region/store/guide headers) now drive headquarters/store/guide visibility. The prototype frontend reads `/api/customer-ops/bootstrap`, loads customer detail from `/api/customers/{id}`, and writes stage, tags, followups, tasks, and materials through real APIs with local fallback. Backend tests cover stage flow, tag add, followup/task side effects, overdue exception resolution, exception generation, and guide-role data filtering.
- 2026-07-06: Migrated the `客户运营中心` from memory-only API toward PostgreSQL persistence. Migration `011_customer_ops_persistence.sql` extends existing `customers`, `tags`, and `customer_tags` for operation fields and adds `customer_lifecycle_logs`, `follow_up_records`, `sop_tasks`, `sop_task_logs`, `customer_operation_timeline`, `materials`, and `operation_exceptions` with demo seed data. PostgreSQL mode now routes customer-operation APIs through split files for permissions, repository reads, service transactions, and DB handlers while memory mode remains the fallback.
- 2026-07-06: Customer-operation transaction rules are now implemented in DB mode: stage changes update `customers`, write `customer_lifecycle_logs`, and append `customer_operation_timeline`; followups insert `follow_up_records`, update `last_follow_up_time`, optionally trigger stage transitions and next SOP tasks; tag adds use the existing unique `customer_tags(customer_id, tag_id)` relation and append timeline records; task completion updates `sop_tasks`, writes `sop_task_logs`, appends timeline records, and resolves related `operation_exceptions`.
- 2026-07-06: Customer-operation data scope is centralized via request headers. `headquarters_admin`/`hq_admin` can see all data, `headquarters_operator` can be region-scoped, `regional_manager` filters by `region_id`, `store_owner`/`store_manager` filters by `store_id`, and `guide` filters by `owner_guide_id` or task assignee. DB customer detail returns `403 forbidden` when the record exists but is outside scope. PostgreSQL DB tests run when `SCRM_TEST_DATABASE_URL` is set; regular `go test ./...` skips those tests without a DB.
- 2026-07-06: Closed the local production-style dev environment loop. `.env.example` now documents `DATABASE_URL`, `SCRM_TEST_DATABASE_URL`, `SCRM_DATABASE_MODE`, `BACKEND_PORT`, and `FRONTEND_PORT`; root npm scripts and `Makefile` cover memory dev, DB dev, migrate, seed, status, backend tests, DB tests, and build. `.env` is loaded as defaults via `scripts/run-with-env.mjs`, so explicitly passed environment variables win. Customer-operation demo data moved from migration into `backend/seeds/customer_ops_demo_seed.sql`; migrations now record `schema_migrations` checksums and `scrm-admin` provides `migrate/seed/status`. `/api/health` and `/api/system/status` expose storage mode, DB connection, pool, and migration state, and the prototype shows a small dev status badge. Do not reset or delete the existing local PostgreSQL volume on host port `15432`; current verification used a disposable PostgreSQL container on `15433`.
- 2026-07-06: Entered the MVP acceptance and product-documentation closeout phase. Added `docs/SCRM_PRODUCT_SPEC.md`, `docs/SCRM_ACCEPTANCE_CHECKLIST.md`, and `docs/SCRM_DEMO_SCRIPT.md` so the current SCRM MVP is explainable, testable, and demoable without adding new large business modules. README now links to those documents. Current product direction is to freeze the MVP boundary and avoid new modules such as marketing campaigns, full community operations, AI assistants, complex BI, tenant packaging, and mobile until the existing acceptance path is signed off. The recommended next stage is an Enterprise WeChat integration center covering config/doctor checks, department/member sync, external contact sync, contact-way sync, group sync, callback validation/decryption/idempotency/retry/dead-letter handling, and permission hardening.
- 2026-07-06: Started industrialization Phase 2 as an Enterprise WeChat integration-center foundation, not full customer sync. Added `/api/scrm/wecom/status` for sanitized config, access-token cache state, local asset counts, retry summary, latest callback, doctor summary, and next-step suggestions; added `/api/scrm/wecom/error-dictionary` for common Enterprise WeChat errcode explanations and remediation hints; `/api/system/status` now includes a compact `wecom` summary. API errors now return structured `{code,message,requestId,details}` payloads, and WeCom API errcodes such as 60011, 60020, 42001, and 45009 map to stable local codes and suggestions. Next Phase 2 work should connect this status model to the frontend integration page and then proceed to department/member/contact-way sync.
- 2026-07-06: Completed industrialization Phase 2B as an Enterprise WeChat config and permission-check loop, still not full customer/group/contact-way sync. Added `wecom_configs`, `wecom_tokens`, and `wecom_permission_checks`; implemented `GET/PUT /api/scrm/wecom/config`, `POST /api/scrm/wecom/test-token`, `POST /api/scrm/wecom/permission-check`, and `GET /api/scrm/wecom/permission-checks` in memory and PostgreSQL modes. Secrets are encrypted/masked at rest and never returned in full; access tokens are cached but not exposed. Permission checks cover contact/customer/app token, department read, user read, and customer-contact basic permission, while customer-group/contact-way sync checks remain planned/not_checked. Failed checks create/update operation exceptions such as `wecom_permission_error`, `wecom_token_error`, `wecom_ip_whitelist_error`, and `wecom_rate_limit_error`. The headquarters prototype now shows the integration config card, token status card, permission-check history, and actions for config save, token test, and permission check.
- 2026-07-06: Added an explicit test-data safety rule for future E2E and manual acceptance testing. Prefer temporary PostgreSQL containers or `SCRM_TEST_DATABASE_URL`; never write E2E data into real `DATABASE_URL`, never reset/delete the existing `15432` volume, and require every E2E-created customer/store/guide/tag/task/exception/WeCom record to include a unique `test_run_id` such as `e2e_YYYYMMDD_HHMMSS`. Added `scripts/e2e-cleanup.mjs` plus npm/Makefile targets for cleanup dry-run/apply. The cleanup script refuses port `15432`, refuses non-test DB names by default, only deletes rows matching the supplied `test_run_id`, reports dry-run/deleted/remaining counts, and fails if matching rows remain after apply. `npm run test:backend:db` now also requires an explicit `SCRM_TEST_DATABASE_URL` and refuses port `15432`. Full rules live in `docs/SCRM_TEST_DATA_SAFETY.md`.

## Open Questions

- None currently.
